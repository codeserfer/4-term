1) Переопределить Метод Equals для reference-type и для value-type

2) Для Type1(Int32, String, Boolean) определить оператор + и метод ToString

3) Показать вызовы virtual (как вызываются методы для virtual, new и override)

4) В функцию можно передавать заранее неизвестное число параметров. Например, функция может складывать некоторое заранее неизвестное количество чисел и возвращать их сумму.

5) Сделать расширитель для типа String. Метод, возвращающий массив числел, которые найдены в строке. Числа могут находится в строке через запятую.

6) Есть класс Man (определить св-ва Имя, Возраст, метод ToString (отображать ("Человек, Имя, Возраст").
   Есть класс Teenager (переопределить свойство Возраст - наложить ограничение на значения (13..19) и ToString (отображать текст "Подросток, Имя, Возраст").
   Есть класс Worker (переопределить сво-во Возраст (наложить ограничение на значение (16...70) и ToString(отображать "Работник, Имя, Возраст").
   Есть класс СписокЛюдей (определить массив Людей внутри класса и определить индексатор для доступа к тому или иному человеку). ToString - информация должна отображаться корректно - для человека - "Человек", для подростка - "Подросток" и т.п.

7) Проверить скорость работы с инетрнированием строки. Посмотреть, как изменяется скорость, если строка интернируется и не интернируется через метод Intern. Сравнить на достаточно большом количестве символов.
   Проверить скорость работы конкатенации строки для типа String и StringBuilder. Каждый раз к строке прибавляется от 1 до 20 символов. Посмотреть и сравнить скорость работы программы для разной длины прибавляемой строки.

8) Пользователь вводит количество измерений массива и значения верхних и нижних границ (CreateInstance). После заполняет массив числами (SetValue). После заполнения посчитать произведение всех чисел (GetVelue).

9) Отобразить все варианты вывода текущей даты, значений типа int, double, enum. Использовать String.Format для вывода. Сделать ввод нескольких вариантов даты, числа через метод Parse с форматной строкой. Отобразить дату для нескольких культур.
